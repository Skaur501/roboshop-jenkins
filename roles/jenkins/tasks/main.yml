- name: Create jenkins jobs
  hosts: localhost
  tasks:
  - name: Create a jenkins jobs using  basic authentication
    community.general.jenkins_jobs:
      config: "{{ lookup('file', 'templates/single-pipeline.xml') }}"
      name: test
      password: admin
      url: https://172.31.12.47:8080
      user: admin


